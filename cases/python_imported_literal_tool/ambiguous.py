from agents import Agent
from pkg.tools import imported_writer as duplicate_local
from pkg.tools import imported_writer as duplicate_local


ambiguous_agent = Agent(name="duplicate-local-import", tools=[duplicate_local])
